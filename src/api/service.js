// src/api/service.js
// Entry point bound to the Express API microservice. It simply imports
// the main server bootstrap so we have a clear executable for container
// schedulers (mirrors src/worker/service.js).

import "../index.js";
